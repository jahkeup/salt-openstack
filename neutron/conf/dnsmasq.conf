{% set quantum = pillar['openstack']['quantum'] -%}
log-facility = /var/log/quantum/dnsmasq.log
log-dhcp

dhcp-option=option:nis-domain,{{ quantum['network']['domain'] }}
dhcp-option=option:dns-server,{{ quantum['network']['dns_server'] }}

